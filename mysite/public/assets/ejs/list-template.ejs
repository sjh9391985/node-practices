<% for(let ls of list) { %>	
	<li data-no='<%=ls.no%>'> 
		<strong><%=ls.name%></strong>
		<p><%=ls.message.replace(/\n/gi, "<br/>")%></p>
		<strong></strong> 
		<a href='' data-no='<%=ls.no%>'>삭제</a> 
	</li>
<%
	}
%>